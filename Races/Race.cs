using System.Collections;
using UnityEngine;

namespace Assets.Scripts.Races
{
	public class Race : MonoBehaviour
	{
		// Use this for initialization
		private void Start()
		{
			var list = new ArrayList(GetComponents<RaceBuildingEntry>());

			// 
		}

		// Update is called once per frame
		private void Update()
		{

		}
	}

}
